import React from "react";
import { Link, withRouter, Route, Switch } from "react-router-dom";
import { connect } from "react-redux";

const GraphSpeedPerLanguage = props => {
    return (
        <div>GraphSpeedPerLanguage</div>
    )
}

export default withRouter(GraphSpeedPerLanguage);